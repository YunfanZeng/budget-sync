import React from 'react';
import { View } from 'react-native';
const Graph = () => {
    return (
        <View style={{backgroundColor: 'green', flex: 1, borderRadius: 10, margin:2}} />
    )
}

export default Graph