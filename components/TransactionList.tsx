import React from 'react';
import { View } from 'react-native';

const TransactionList = () => {
    return (
        <View style={{backgroundColor: 'red', flex: 1, borderRadius: 10, margin:2}} />
    )
}

export default TransactionList